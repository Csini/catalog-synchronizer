spring:
  kafka:
    streams:
      auto-startup: false
    bootstrap-servers : ${spring.kafka.bootstrap-servers}
aggregateWindowInSec: 0
flushSize: 0
path:
  input: /input
directory:
  output:
    name: /../test-output
store:
  name:
    productPair: productPairStore
topic:
  name:
    readedFromDb: readedFromDb
    validProduct: validProduct
    invalidProduct: invalidProduct
    flushed: flushed
    product: productEvents
    dbevent: dbEvents
    runs: runs